interface:
  display_name: "GitHub PR Review"
  short_description: "Revisa PRs do FILO com evidências e duas passagens"
  default_prompt: "Use $github-pr-review para revisar o PR 75 do repositório atual em modo isolado."

policy:
  allow_implicit_invocation: false
